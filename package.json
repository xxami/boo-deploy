{
  "name": "boo-deploy",
  "version": "1.0.0",
  "private": true,
  "description": "spooky discord bot web server",
  "repository": "https://github.com/xxami/boo-deploy.git",
  "license": "UNLICENSED",
  "author": "lilah",
  "scripts": {
    "server": "node boo.js",
    "test": "./node_modules/.bin/mocha --harmony --reporter spec --require should *test.js"
  },
  "dependencies": {
    "config": "^1.27.0",
    "github-webhook-handler": "^0.6.0",
    "js-yaml": "^3.10.0"
  },
  "devDependencies": {
    "mocha": "^3.4.2",
    "should": "^11.2.1",
    "supertest": "^3.0.0"
  }
}
